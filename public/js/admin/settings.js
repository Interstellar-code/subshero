// Instantiate this class
$(document).ready(function () {
    app.register('settings', new clsApp_Settings);
});



class clsApp_Settings {
    e = {
        // Elements

    };
    d = {
        // Default values

    };
    c = {
        // Configuration

    };

    constructor() {
        // lib.def('smtp', new clsApp_Settings_SMTP(), false, this);
        // lib.def('template', new clsApp_Settings_Template(), false, this);
        // lib.def('update', new clsApp_Settings_Update(), false, this);
    }

    init() {
    }

}

